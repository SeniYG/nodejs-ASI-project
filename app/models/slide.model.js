
/*module.exports(type);
module.exports(title);
module.exports(id);
module.exports(fileName);

var data

function SlidModel (type, id, title, fileName, data) {
	this.type = type;
	this.id = id;
	this.title = title;
	this.fileName = fileName;
	this._data = data;
}

SlidModel.prototype.getData = function() {
	return this.data;
};

SlidModel.prototype.setData = function(data) {
	this.data = data;
};

SlidModel.prototype.create = function(slid, callback) {
	
};

SlidModel.prototype.read = function(id, callback) {
	
};

SlidModel.prototype.update = function(slid, callback) {
	
};

SlidModel.prototype.delete = function(id, callback) {
	
};
module.exports = SlidModel;
*/
